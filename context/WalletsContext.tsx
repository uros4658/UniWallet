// WalletsContext.tsx
import { createContext } from 'react';

const WalletContext = createContext({
  wallet: null,
// This is a placeholder function. It will be replaced by the actual function in the provider.
});

export default WalletContext;